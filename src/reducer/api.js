import { CALL_API } from '../action/api';

const initialState = {};

const api = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default api;
